<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Allow access to external files directory (for camera temp files) -->
    <external-files-path
        name="external_files"
        path="Pictures" />
    <!-- Allow access to cache directory (for sharing temp files) -->
    <cache-path
        name="cache"
        path="." />
</paths>